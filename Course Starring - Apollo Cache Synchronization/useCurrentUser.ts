// Placeholder — in the real app this reads the logged-in user from an
// existing Apollo query or auth context, not a hardcoded value.
export function useCurrentUser() {
  return { id: 'user-1' };
}
